App\Entity\User:
  user_template (template):
    id: "@=Symfony\\Component\\Uid\\UuidV6::v6()"
    username (unique): '<username()>'
    email: '<email()>'
    password: '<password()>'
    # account_status: '@season_*'

  user_admin_{1..3} (extends user_template):
    email: 'aaaaaaaaaaaaaaaaaaaaaaaaaaadmin\@example.com'
    password: 'adminMdp'

  user_with_subscription_{1..10} (extends user_template):
    email: '<email()>'
    currentSubscription: '@subscription_*'

  user_without_subscription_{1..50} (extends user_template):